extends: default
ignore: |
  .venv/
  lakehouse/
  dbt/target/
  dbt/dbt_packages/
  airflow/.astro/
rules:
  line-length:
    max: 120
    level: warning
  truthy:
    check-keys: false
  comments:
    min-spaces-from-content: 1
  document-start: disable
